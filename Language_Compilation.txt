// Get suivant Pseudoooototo Code
Getsuivant : Liste <string> suivant // ce que l'on veut calculer
nom = regle.getnom(); // on recup le nom de la regle que l'on veut calculer
suivant.add('$') // on ajoute $ de base
while(foreach(regle)) // on parcours les regles
	{
	while(foreach(regle.valeur)) // on parcours les valeurs des regles une à une
		{
		if (nom = regle.valeur && regle.valeur.at()+1 != '\'') // Si le nom de la regle a calculer a été trouvé dans les valeurs et que le suivant n'est pas un '
			{
			string str = regle.valeur.at()+1; // le caractere qui suit le nom de la regle dans la valeur
			if(regle.valeur.at()+2 == '\'') str = regle.valeur.at()+2; // si le nom de la regle comportait un '
			}
		}
	if(at()nom == size-1 && nom!=regle.getnom()) // Si Le nom que l'on cherche est le dernier caractere de la liste des valeurs et que le nom de la regle en cours n'est pas le nom de la regle que l'on calcule
		{
		suivant += getsuivant(regle);
		}else if(isregle(str)){ // si l'état de str est non terminal
			suivant += getpremier(str); // on calcule son premier
			if(suivant.comp('#')) // si il y a le mot vide dans suivant : /// check si le mettre ailleurs n'est pas une meilleure idée
				{
				suivant += getsuivant(getregle()); // on ajoute le suivant de la regle en cours a la regle que l'on veut calculer
				suivant.remove('#'); // on retire le mot vide
				}
		}else if (str == '#'){ // si str n'est pas une regle mais est le mot vide
			suivant += suivant(getregle()); // on prend le suivant de la regle actuelle (/// faut-il verifier si la regle actuelle est différente de la regle que l'on veut calculer ?)
		}else{ // si str n'est ni une regle ni le mot vide alors c'est une valeur terminale
			suivant.add(str); // donc on l'ajoute a la liste suivant.
		}
	}
}
							        / \
// Gaffe au foreach de foreach qui font se perdre le programme /_!_\